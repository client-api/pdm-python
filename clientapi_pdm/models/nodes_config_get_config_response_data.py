# coding: utf-8

"""
    Proxmox Datacenter Manager API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pdm.proxmox.com/docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from clientapi_pdm.models.pdm_default_lang_enum import PdmDefaultLangEnum
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class NodesConfigGetConfigResponseData(BaseModel):
    """
    NodesConfigGetConfigResponseData
    """ # noqa: E501

    ciphers_tls_1_2: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="OpenSSL cipher list used by the api server for TLS <= 1.2", alias="ciphers-tls-1.2")

    ciphers_tls_1_3: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="OpenSSL ciphersuites list used by the api server for TLS 1.3", alias="ciphers-tls-1.3")

    default_lang: Optional[PdmDefaultLangEnum] = Field(default=None, description="All available languages in Proxmox. Taken from proxmox-i18n repository. pt_BR, zh_CN, and zh_TW use the same case in the translation files.", alias="default-lang")

    email_from: Optional[Annotated[str, Field(min_length=2, strict=True, max_length=64)]] = Field(default=None, description="E-Mail Address.", alias="email-from")

    http_proxy: Optional[Annotated[str, Field(min_length=1, strict=True, max_length=128)]] = Field(default=None, description="HTTP proxy configuration [http://]<host>[:port]", alias="http-proxy")

    __properties: ClassVar[List[str]] = ["ciphers-tls-1.2", "ciphers-tls-1.3", "default-lang", "email-from", "http-proxy"]


    @field_validator('ciphers_tls_1_2')
    def ciphers_tls_1_2_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[0-9A-Za-z_:, +!\-@=.]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9A-Za-z_:, +!\-@=.]+$/")
        return value


    @field_validator('ciphers_tls_1_3')
    def ciphers_tls_1_3_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[0-9A-Za-z_:, +!\-@=.]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9A-Za-z_:, +!\-@=.]+$/")
        return value



    @field_validator('email_from')
    def email_from_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[[:^cntrl:]]*$", value):
            raise ValueError(r"must validate the regular expression /^[[:^cntrl:]]*$/")
        return value




    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of NodesConfigGetConfigResponseData from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            # `exclude_unset` keeps schema defaults out of the wire payload
            # when the user constructed the model directly (e.g.
            # `Req(vmid=100)` would otherwise pull in
            # `cores=1, cpulimit=0, …` from the spec defaults and PVE
            # rejects the request with 400 because it never set those).
            # `exclude_none` keeps None values out of the wire payload —
            # both for direct construction (None means "unset") and for
            # the from_dict path (where unspecified obj keys become
            # `obj.get("k") == None` but show up in `model_fields_set`).
            exclude_unset=True,
            exclude_none=True,
        )
        
        
        
        
        
        
        
        
        
        
        
        
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of NodesConfigGetConfigResponseData from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "ciphers-tls-1.2": obj.get("ciphers-tls-1.2"),
            "ciphers-tls-1.3": obj.get("ciphers-tls-1.3"),
            "default-lang": obj.get("default-lang"),
            "email-from": obj.get("email-from"),
            "http-proxy": obj.get("http-proxy")
        })
        return _obj


